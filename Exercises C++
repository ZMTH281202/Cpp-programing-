#include <stdio.h>
#include <math.h>
int main()
{
	int n, f=1;
	printf("enter a number: ");
	scanf("%d", &n);
	
	if (n<0)
	printf("Number is negative" );
	else 
	{
		for (int i=1; i<=n; i++)
			f =f*i;
		printf("Factorial = %d", f);		
	}	
}
